function squareSum(numbers){
 let newArr = [];

    for (let i = 0, len = numbers.length; i < len; i++) {
        newArr.push(numbers[i] ** 2);
    }

    let total = newArr.reduce(function(a,b){return a + b},0);
    return total;
}
